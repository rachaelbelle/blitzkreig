<div id="profile">
  <div class="container">
    <div class="row">
      <div class="col-s12">
        <h1>Blitzkreig!</h1>
<<<<<<< HEAD
        {{title}}
        {{#each}}
          {{data}}
        {{/each}}
=======

        <div>
         {{#each blitzdb}}
            {{#if weather}}
              {{> widgets/weather-block }}
            {{/unless}}
          {{/each}}
        </div>

        <div>
          {{#each blitzdb}}
            {{#if traffic}}
              {{> widgets/traffic-block}}
            {{/if}}
          {{/each}}
        </div>

        <div>
          {{#each blitzdb}}
            {{#if news}}
              {{> widgets/news-block}}
            {{/if}}
          {{/each}}
        </div>

        <div>
          {{#each blitzdb}}
            {{#if quote}}
              {{> widgets/quote-block}}
            {{/if}}
          {{/each}}
        </div>

>>>>>>> 305072e9e06011f55f5996c9c910665259a93016
      </div>
    </div>
  </div>
</div>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<script src="/js/index.js"></script>
