language: node_js
node_js: '8'
services:
  - mysql
addons:
  apt:
    sources:
      - mysql-5.7-trusty
    packages:
      - mysql-server
      - mysql-client
branches:
  only:
  - master
cache:
  directories:
    - node_modules
before_install:
  - mysql -e ‘CREATE DATABASE IF NOT EXISTS testdb;’
